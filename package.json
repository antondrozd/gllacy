{
  "name": "Gllacy",
  "version": "0.1.0",
  "private": true,
  "description": "Магазин мороженного",
  "devDependencies": {
    "autoprefixer": "6.7.x",
    "browser-sync": "2.18.12",
    "css-mqpacker": "^6.0.1",
    "del": "^3.0.0",
    "gulp": "3.9.1",
    "gulp-csso": "^3.0.0",
    "gulp-imagemin": "^3.3.0",
    "gulp-plumber": "1.1.0",
    "gulp-postcss": "6.4.0",
    "gulp-rename": "^1.2.2",
    "gulp-sass": "3.1.0",
    "gulp-sequence": "^0.4.6",
    "gulp-svgmin": "^1.2.4",
    "gulp-svgstore": "^6.1.0",
    "gulp-ttf2woff": "^1.1.1",
    "run-sequence": "^1.2.2"
  },
  "scripts": {
    "test": "editorconfig-cli",
    "build": "gulp build",
    "start": "npm run build && gulp serve",
    "new": "gulp new:html && mkdir sass && mkdir js && mkdir fonts && mkdir libs && mkdir img && cd img && mkdir icons && cd .. && cd sass && mkdir blocks && touch scaffolding.scss && touch style.scss && touch variables.scss && touch fonts.scss && cd .. && cd js && touch script.js"
  },
  "editorconfig-cli": [
    "*.html",
    "*.json",
    "*.js",
    "js/**/*.js",
    "img/**/*.svg",
    "sass/**/*.{sass,scss}"
  ],
  "engines": {
    "node": "6.10"
  }
}
